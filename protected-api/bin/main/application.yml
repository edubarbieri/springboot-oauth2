server:
  port: 8383

spring:
  application:
    name: proteced-api

security:
  oauth2:
    resource:
      token-info-uri: http://localhost:8282/oauth/check_token
    client:
      client-id: webapp
      client-secret: test12